// anonymous enum

#include<stdio.h>
enum {Red,Blue,Green,Yellow,White,Pink} c1;
// here c1 is the variable of anonymous enum

void main()
{
    typedef enum {Marketing=1,Training,Department} dept;
    // dept is the name of the anonymous enum
    dept d1;
    // d1 is the variable
    
}