/*
    Storage Classes

    auto / local variable
                                    Storage == Stack
                                    Initial VAlue == Garbage
                                    Life == Block
                                    Scope == Block

                            
    Register variable
                                    Storage == CPU Register
                                    Initial VAlue == Garbage
                                    Life == Block
                                    Scope == Block
                            

    Static variable
                                    Storage == Data Section
                                    Initial VAlue == Zero
                                    Life == Program
                                    Scope == Limited
                            

    Extern / Global variable
                                    Storage == Data Section
                                    Initial VAlue == Zero
                                    Life == Program
                                    Scope == Program


*/