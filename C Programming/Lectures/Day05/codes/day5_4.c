// bitwise shift ops : << (left shift) and >> (right shift)
#include<stdio.h>

int main()
{
    int num = 10;

    printf("num << 2 = %d\n",num << 2);
    printf("num >> 2 = %d\n",num >> 2);
    return 0;
}